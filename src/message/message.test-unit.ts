import {
  extractMessage,
} from './message.js';

describe('extractMessage', () => {
  beforeAll(() => { });

  afterAll(() => { });

  beforeEach(() => { });

  afterEach(() => { });

  test.skip('can calculate 2 plus 2', () => {
    expect(2 + 2).toBe(4);
  });
});
